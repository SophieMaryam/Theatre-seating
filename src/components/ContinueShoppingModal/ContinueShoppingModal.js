import Vue from "vue";

export default Vue.extend({
    name: "ContinueShoppingModal",
    data() {
        return {
            data: {
                showModal: false
            }
        }
    }
});